<Window x:Class="QuinkGest.Views.DepensesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dépenses" Height="520" Width="720"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Nouvelle dépense" Grid.Row="0" Padding="8" Margin="0,0,0,8">
            <WrapPanel>
                <TextBox x:Name="ChampDescription" Width="180" Margin="4" Tag="Description"/>
                <TextBox x:Name="ChampCategorie" Width="130" Margin="4" Tag="Catégorie"/>
                <TextBox x:Name="ChampMontant" Width="100" Margin="4" Tag="Montant"/>
                <Button x:Name="BoutonAjouter" Content="Ajouter" Margin="4" Padding="8,4" Click="BoutonAjouter_Click"/>
                <Button x:Name="BoutonSupprimer" Content="Supprimer" Margin="4" Padding="8,4" IsEnabled="False" Click="BoutonSupprimer_Click"/>
            </WrapPanel>
        </GroupBox>

        <TextBlock x:Name="TexteTotalMois" Grid.Row="1" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>

        <DataGrid x:Name="GrilleDepenses" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionChanged="GrilleDepenses_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding DateDepense, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="220"/>
                <DataGridTextColumn Header="Catégorie" Binding="{Binding Categorie}" Width="150"/>
                <DataGridTextColumn Header="Montant" Binding="{Binding Montant}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
